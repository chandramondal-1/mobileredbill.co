import { HeroSection, RosterSection, FlavorMatrix, TechSpecs } from '@/sections'

export default function Home() {
  return (
    <main className="relative">
      <HeroSection />
      <RosterSection />
      <FlavorMatrix />
      <TechSpecs />
    </main>
  )
}
